# Personal Data Engineering Project

## Overview
This repo is a template for personal data engineering projects using:
- Python, Pandas, SQL (PostgreSQL)
- Snowflake
- Airflow DAGs
- AWS integration
- Tableau (versioned workbooks stored separately)

## Setup
1. Create virtual environment:
   ```bash
   python -m venv venv
   source venv/Scripts/activate  # Windows
   pip install -r requirements.txt